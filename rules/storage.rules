rules_version = "2";

service cloud.storage {
	match /b/{bucket}/o {
		match /{allPaths=**} {
			allow read, write: if false;
		}
	}

	match /b/knowledge/o {
		match /{allPaths=**} {
			allow read, write: if false;
		}
	}
}