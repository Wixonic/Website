{
	"scripts": {
		"serve": "clear; npm run build; firebase emulators:start",
		"prepublish": "clear; npm run build",
		"publish": "clear; firebase publish",
		"build": "clear; rm -r ../build/; npx @11ty/eleventy"
	},
	"dependencies": {
		"@11ty/eleventy": "^3.1.2"
	},
	"type": "module"
}