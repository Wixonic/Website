{
	"scripts": {
		"serve": "clear; npm run testbuild; cd .. && firebase emulators:start; cd src",
		"prepublish": "clear; npm run build",
		"publish": "clear; cd .. && firebase deploy; cd src",
		"build": "clear; rm -rf ../build/; npx @11ty/eleventy",
		"testbuild": "dev=true clear=true npm run build"
	},
	"dependencies": {
		"@11ty/eleventy": "3.1.2",
		"esbuild": "0.27.3",
		"html-minifier": "4.0.0"
	},
	"type": "module"
}
