{
	"scripts": {
		"serve": "clear; dev=true npm run build; cd .. && firebase emulators:start; cd src",
		"prepublish": "clear; npm run build",
		"publish": "clear; cd .. && firebase deploy; cd src",
		"build": "clear; rm -r ../build/; npx @11ty/eleventy"
	},
	"dependencies": {
		"@11ty/eleventy": "^3.1.2"
	},
	"type": "module"
}